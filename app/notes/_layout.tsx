import { Stack } from "expo-router";

function NoteLayout(){
      return <Stack
    screenOptions={{
      //main page 
      contentStyle: {
        backgroundColor: 'blue',
      }
    }}>
    </Stack>;
};

export default NoteLayout;